# Secrets / credentials — never commit
.env
gmail_token.pkl
credentials.json
.greennode.json
.agentbase-state.json

# Python
__pycache__/
*.pyc

# Local editor/agent settings
.claude/settings.local.json

# External cloned repo (has its own git history)
greennode-agentbase-skills/

# Scratch/temp
_*.txt
*.tmp
~$*

# Submission bundle (contains LLM key — do not commit)
submission.zip
*.zip
.agentbase/
_*.json
